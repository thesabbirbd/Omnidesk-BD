import React from 'react';

export default function StudyPlan() {
  return (
    <div className="flex items-center justify-center h-full border border-[var(--border-color)] rounded-2xl bg-[var(--bg-panel)] neumorphic-panel">
      <h1 className="text-2xl font-semibold text-[color:var(--text-muted)] border-b border-[var(--border-color)] pb-2">
        StudyPlan Page
      </h1>
    </div>
  );
}
